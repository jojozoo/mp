日期：<%= time_style(@photo.created_at) %>
<br>
活动：<%= @photo.event ? "##{@photo.event.name}#" : '未参加' %>
<br>
作品：<%= @photo.work ? "##{@photo.work.title}#" : '无' %>
<br>
相册：<%= @photo.album ? @photo.album.name : '未发布' %>
<br>
用户：<%= link_to @photo.user.username, admin_user_path(@photo.user_id), target: '_blank' %>(<%= @photo.user.fols_count %>)
<br>
动态：喜欢(<%= @photo.liks_count %>) 收藏(<%= @photo.stos_count %>) 编辑推荐(<%= @photo.recs_count %>)
<br>
操作：<%#= @photo.tuis.map(&:channel).join(",") %>