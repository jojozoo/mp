日期：<%= time_style(@image.created_at) %>
<br>
活动：<%= @image.event ? "##{@image.event.name}#" : '未参加' %>
<br>
作品：<%= @image.work ? "##{@image.work.title}#" : '无' %>
<br>
相册：<%= @image.album ? @image.album.name : '未发布' %>
<br>
用户：<%= link_to @image.user.username, admin_user_path(@image.user_id), target: '_blank' %>(<%= @image.user.followers_count %>)
<br>
动态：喜欢(<%= @image.likes_count %>) 收藏(<%= @image.stores_count %>) 推荐(<%= @image.recoms_count %>)
<br>
操作：<%= @image.pushes.map(&:channel).join(",") %>